name: CodeQL

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'tests/**'
      - 'CMakeLists.txt'
      - '.github/workflows/codeql.yml'
  # PR では paths フィルタを付けない。必須ステータスチェックに設定している
  # ため、paths でワークフローがスキップされるとチェックが報告されず、PR が
  # マージ不能になるのを防ぐ（ブランチ保護のゲートを確実に機能させる）。
  pull_request:
    branches: [main]

jobs:
  codeql:
    name: CodeQL Analysis
    runs-on: ubuntu-24.04

    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup GCC 14
        run: |
          sudo apt update
          sudo apt install -y gcc-14

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: cpp
          queries: security-and-quality
          build-mode: manual

      - name: Configure CMake
        run: |
          cmake -B build/ci \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_COMPILER=gcc-14 \
            -DPORTABLE_BUILD=ON \
            -DENABLE_LTO=OFF \
            -DENABLE_PGO_GENERATE=OFF \
            -DENABLE_PGO_USE=OFF \
            -DENABLE_GRAPHITE=OFF

      - name: Build for CodeQL
        run: cmake --build build/ci -j"$(nproc)"

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
